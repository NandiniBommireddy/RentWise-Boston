# 1.x: sampling params removed, output_config replaces raw output_format dicts.
anthropic>=1,<2
duckdb>=1.1.0
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-dotenv>=1.0.0

# Local ONNX embeddings for the dense half of hybrid search.
# Optional: the backend degrades to BM25-only if this import fails.
fastembed>=0.4.0
